apiVersion: v1
kind: Service
metadata:
  name: conotoso-3
  namespace: dev
  labels:
    app.kubernetes.io/name: conotoso-3
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: conotoso-3
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000